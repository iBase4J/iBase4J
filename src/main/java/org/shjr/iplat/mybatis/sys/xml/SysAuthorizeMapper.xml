<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.shjr.iplat.mybatis.sys.dao.SysAuthorizeMapper">
	<delete id="deleteUserMenu">
		delete from sys_user_menu where user_id=#{user_id}
	</delete>
	<delete id="deleteUserRole">
		delete from sys_user_role where user_id=#{user_id}
	</delete>
	<delete id="deleteRoleMenu">
		delete from sys_role_menu where role_id=#{role_id}
	</delete>
	<select id="getAuthorize" resultType="org.shjr.iplat.mybatis.generator.model.SysMenu">
		select m.* from sys_menu m
		where exists (select 1 from sys_user_menu um where um.user_id=#{user_id} and um.menu_id=m.menu_id)
		or exists (select 1 from sys_user_role ur join sys_role_menu rm on ur.role_id=rm.role_id
		 where ur.user_id=#{user_id} and rm.menu_id=m.menu_id)
	</select>
</mapper>